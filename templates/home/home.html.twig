{% extends 'base.html.twig' %}

{% block body %}
    <h1>Home</h1>
      {% for gig in gigs %}
        <div class="row">
          <div class="col-md-8">
            <h2>{{ gig.title }}</h2>
            <p>{{ gig.description }}</p>
            <p><a class="btn btn-default" href="" role="button">View details &raquo;</a></p>
          </div>
          <div class="col-md-4">
            <h2>Heading</h2>
            <p>...</p>
            <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
          </div>
        </div>
      {% endfor %}
    <p>Home page</p>
{% endblock %}
